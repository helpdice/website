// import type { LocalePrefixMode } from 'next-intl/types/routing/types';

// const localePrefix = 'as-needed';

// FIXME: Update this configuration file based on your project information
export const AppConfig = {
  name: 'Helpdice',
  locales: ['en'],
  defaultLocale: 'en',
  // localePrefix,
};
